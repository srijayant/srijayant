{
  "name": "srijayant-profile",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling to author, lint, and preview the GitHub profile README.",
  "type": "module",
  "scripts": {
    "lint": "markdownlint-cli2",
    "preview": "node scripts/preview.mjs",
    "build": "node scripts/preview.mjs --build"
  },
  "devDependencies": {
    "github-markdown-css": "5.9.0",
    "markdownlint-cli2": "0.23.2",
    "marked": "18.0.12"
  }
}
